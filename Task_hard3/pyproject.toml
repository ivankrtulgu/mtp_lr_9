[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "task_hard3"
version = "0.1.0"
description = "Rust module with is_prime function"
requires-python = ">=3.9"

[tool.maturin]
features = ["pyo3/extension-module"]
