# Prompt Log

## Задание 2: Добавить горутину для фоновой обработки запросов

### Промпт 1
**Инструмент:** Qwen Code
**Промпт:** Go (Golang). Добавить фоновую обработку через go func() и chan.
Задача: Асинхронная очередь задач. Main thread не блокируется.
Интерфейс: функция ProcessBG(data map[string]any), использование context.Context для таймаутов, sync.WaitGroup для завершения.
Тест: go test, проверка отсутствия блокировки и race condition (-race).
Запрет: Использование unsafe, неконтролируемые утечки горутин.

**Результат:** Получил базовую реализацию асинхронной очереди задач с go func(), каналами, context.Context для таймаутов и sync.WaitGroup для завершения; все тесты проходят без race conditions и утечек горутин.

### Промпт 2
**Инструмент:** Qwen Code
**Промпт:** Задача: Переписать тесты с Go на Python (pytest).
Исходный код: [Вставлен ранее полученный Go-код]
Требования:
Библиотека: pytest, pytest-asyncio.
Аналоги: 
- go func() -> asyncio.create_task или threading.Thread.
- chan -> asyncio.Queue или queue.Queue.
- atomic -> threading.Lock и int.
Имена функций: Сохранить смысл (например, test_process_bg_non_blocking).
Стиль: Лаконичный Python, без лишних зависимостей.
Выход: Один файл test_taskqueue.py.

**Результат:** Успешно портировал тесты с Go на Python (pytest). Все 13 тестов проходят успешно, логика синхронизации и конкурентности сохранена.

### Итого
- **Количество промптов:** 2
- **Что пришлось исправлять вручную:** Изменил тип счетчика в Python-тестах, так как AI изначально предложил бесконечный генератор для проверки атомарных операций.
- **Время:** ~15 мин

---

## Задание 4: Передача данных из Python в Go через JSON (stdin/stdout)

### Промпт 1
**Инструмент:** Qwen Code
**Промпт:** Go + Python. Обмен данными через JSON (stdin/stdout).
Задача: Python отправляет JSON в Go, Go обрабатывает и возвращает JSON в stdout.
Go: main.go читает os.Stdin, десериализует в struct, пишет результат в os.Stdout.
Python: модуль bridge.py, использует subprocess.Popen (stdin.write / stdout.read).
Тест: test_integration.py на pytest. Передать {"val": 10}, получить {"result": 20}.
Запрет: Временные файлы, только потоки (pipes).

**Результат:** Получил базовую реализацию Go + Python интеграции через JSON (stdin/stdout); Go читает из os.Stdin, десериализует в struct, удваивает значение и пишет в os.Stdout; Python использует subprocess.Popen с pipe'ами; все 4 теста pytest проходят.

### Итого
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** ничего
- **Время:** ~10 мин

---

## Задание 9: Сравнение скорости Rust и Python

### Промпт 1
**Инструмент:** Qwen Code
**Промпт:** Rust + Python. Бенчмарк производительности.
Задача: Сравнить скорость тяжелых вычислений (например, вычисление числа Фибоначчи или обработка большого массива).
Rust: Реализовать функцию в lib.rs, скомпилировать в shared library (.so / .dll).
Python: Вызвать Rust-функцию через ctypes или PyO3. Написать аналог на чистом Python.
Метрики: Использовать timeit для замера 100 итераций. Вывести таблицу сравнения (ms).
Тест: test_benchmark.py проверяет, что результаты функций идентичны и Rust быстрее.

**Результат:** Получил базовую реализацию Rust + Python интеграции через ctypes (shared library .dll); Rust экспортирует `extern "C" fn fibonacci()` для ctypes и PyO3-обёртку; Python загружает .dll через `ctypes.CDLL`, настраивает argtypes/restype; реализованы 3 версии (Rust, Python итеративная, Python рекурсивная); benchmark.py использует timeit для 100 итераций; все 20 тестов pytest проходят (корректность результатов, идентичность вычислений, производительность Rust).



**Метрики:**
- Rust быстрее Python итеративной в 1.39x
- Rust быстрее Python рекурсивной в 228263.10x

### Итого
- **Количество промптов:** 1
- **Что пришлось исправлять вручную:** ничего
- **Время:** ~12 мин